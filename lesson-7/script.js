// task_1

//const getSpecialNumbers = arr => [...arr].filter(num => num % 3 === 0);

// task_2

//const sortDesc = arr => [...arr].sort((a, b) => b - a);

// task_3

//const flatArray = arr => [].concat(...arr).sort((a, b) => a - b);

// task_4

//const getMessagesForBestStudents = (allStudentsList, studentsForRetake) =>
//  [...allStudentsList]
//    .filter(el => !studentsForRetake.includes(el))
//    .map(el => `Good job, ${el}`);

// task_5

//const squareArray = arr => (Array.isArray(arr) ? arr.map(el => el * el) : null);

// task_6

//const reverseArray = arr => (Array.isArray(arr) ? [...arr].reverse() : null);

// task_7

//const increaseEvenEl = (arr, delta) =>
//  Array.isArray(arr) ? arr.map(el => (el % 2 === 0 ? el + delta : el)) : null;

// task_8

//const cloneArr = arr => (Array.isArray(arr) ? [...arr] : null);

// task_9

//const withdraw = (clients, balances, client, amount) => {
//  const index = clients.indexOf(client);

//  return balances[index] >= amount ? (balances[index] -= amount) : -1;
//};

// task_10

//const sum = arr =>
//  Array.isArray(arr) ? arr.reduce((sum, el) => sum + el) : null;

// task_11

//const filterNames = (arr, text) =>
//  arr.filter(word => word.includes(text) && word.length > 5);

// task_12

//const arrAverage = arr =>
//  Array.isArray(arr) ? arr.reduce((acc, ell) => acc + ell) / arr.length : null;
