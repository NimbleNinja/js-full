// algoritm
// need to understand how it works

// input number
// return func => inp : num, return : result(num)
const mult = a => b => a * b;

const twice = mult(2);
const triple = mult(3);

export { twice, triple, mult };
